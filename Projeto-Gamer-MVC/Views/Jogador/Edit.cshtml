@model Jogador

@{
    ViewData["Title"] = "Edit";
}

<!-- MAIN -->
<main>

    <section class="container-grid cadastro-equipes">

        @if (ViewBag.UserName == null)
        {
            <a class="nav-link text-dark" asp-area="" asp-controller="Login" asp-action="Index">Login</a>
        }
        else
        {
            <form class="frm-cad-equipe" method="post" action='@Url.Action("Atualizar")'>
                <div class="cadequipe-titulo">
                    <h1>Atualização de Jogador</h1>
                    <hr>
                </div>

                <input type="hidden" name="IdJogador" value="@ViewBag.Jogador.IdJogador">

                <select class="campo-entrada" name="IdEquipe" id="IdEquipe">
                    <option value="0">Selecione uma opção</option>

                    @foreach (Equipe equipe in ViewBag.Equipe)
                    {
                        <option value="@equipe.IdEquipe">@equipe.Nome</option>
                    }

                </select>

                <input class="campo-entrada" type="text" id="nome-jogador" name="Nome"
                    placeholder="Digite o nome do Jogador:">

                <input class="campo-entrada" type="email" id="email-jogador" name="Email"
                    placeholder="Digite o Email do Jogador:">

                <input class="campo-entrada" type="password" id="senha" name="Seha" placeholder="Digite a Senha:">

                <button type="submit">Atualizar</button>
            </form>
        }
    </section>
</main>

@* <h1>Edição de Jogadores</h1>

@if (ViewBag.UserName == null)
{
    <a class="nav-link text-dark" asp-area="" asp-controller="Login" asp-action="Index">Login</a>
}
else
{
<form method="post" action='@Url.Action("Atualizar")'>
    <input type="hidden" name="IdJogador" value="@ViewBag.Jogador.IdJogador">

    <label for="">Nome</label>
    <input type="text" name="Nome" id="@ViewBag.Jogador.Nome">

    <label for="">Email</label>
    <input type="email" name="email" id="@ViewBag.Jogador.Email">

    <label for="">Senha</label>
    <input type="password" name="Senha" id="@ViewBag.Jogador.Senha">

    <label for="">Equipe</label>
    <select name="IdEquipe" id="IdEquipe">

        @foreach (Equipe equipe in ViewBag.Equipe)
        {
            @if(equipe.IdEquipe == @ViewBag.Jogador.IdEquipe)
            {
                <option value="@equipe.IdEquipe" selected>@equipe.Nome</option>
            }
            else
            {
                <option value="@equipe.IdEquipe">@equipe.Nome</option>
            }
        }
    </select>

    <button type="submit">Atualizar</button>

</form>
} *@
